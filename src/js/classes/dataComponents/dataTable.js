class DataTable {
    constructor(rows, columns, data) {
        this.popup = new Popup('30%', '75%', 0, 0, 'modulePopup', false);
        this.rows = rows;
        this.columns = columns;
        this.data = data;
    };

}